option_settings:
    aws:elasticbeanstalk:application:environment:
        NODE_ENV: production 
container_commands:
    build:
        env:
            NODE_ENV: production
        command: "npm run build"
